// Question:
// Write a function longestUniqueSubstring that takes a string and returns the length of the longest
// substring without repeating characters.

const str = "abcabcbb";

const result = longestUniqueSubstring(str);
console.log(result); // Output: 3 ("abc")
